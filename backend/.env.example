# TRUSTSHIFT Backend Environment Variables
# Copy this file to .env and fill in your values

# MongoDB Atlas Connection String
# Get this from: MongoDB Atlas Dashboard → Connect → Connect your application
# Format: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/

# Database Name
# The name of your MongoDB database (will be created automatically if it doesn't exist)
DATABASE_NAME=trustshift

# Optional: If using local MongoDB instead of Atlas
# MONGODB_URI=mongodb://localhost:27017/

# Note: If MongoDB connection fails, the system automatically falls back to local JSON storage
# Data will be stored in: backend/app/data/local_fallback.json